/autoload/plug.vim
/autoload/plug.vim.old
/plugged

tags
.tags
.vimtags

.netrwhist

# Vim backup, undo, swp folders.
/undo
/swp
/view

# Stub files, so only the dir will be committed (but not the files inside).
!/undo/.empty
!/swp/.empty
